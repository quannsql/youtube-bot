[build]
builder = "NIXPACKS"

[deploy]
# Railway Cron should start this one-shot Short process twice a day.
startCommand = "python -u youtube_shorts_bot.py --publish --scheduled"
restartPolicyType = "NEVER"
