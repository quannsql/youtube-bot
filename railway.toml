[build]
builder = "NIXPACKS"

[deploy]
# Railway Cron starts this one-shot process three times a day.
startCommand = "python -u youtube_shorts_bot.py --publish --scheduled"
restartPolicyType = "NEVER"
